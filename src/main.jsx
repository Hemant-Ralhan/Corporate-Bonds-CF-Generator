import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```
5. Commit

6. Repeat: **Add file** → **Create new file** → name it `src/App.jsx`

7. For this one, open the `cf_generator.jsx` file I gave you earlier in a text editor, copy the entire content, and paste it in

8. Commit

After this your repo should look like:
```
index.html
package.json
vite.config.js
README.md
src/
  App.jsx
  main.jsx
